.venv/
__pycache__/
*.pyc
.env
node_modules/
.next/
celerybeat-schedule*
*.db

# Android build artifacts / IDE state — not needed for Railway (only backend/
# trading_engine/config/docker get deployed), kept out so the repo push
# stays small and fast.
android/.gradle/
android/.idea/
android/.kotlin/
android/build/
android/app/build/
android/local.properties
android/*.iml
android/**/*.iml
